/* global Office */

// For ShowTaskpane buttons, you don't need to associate any functions.
// But the FunctionFile must load successfully (Office.js + no throw).
Office.onReady(() => {
  // no-op
});
